Plamen Vurbanov Abc_example.com (an @ character must separate the local and domain parts)
Plamen Vurbanov Abc@example.com (character dot(.) is last in local part)
Plamen Vurbanov Abc_123@example.com (character dot(.) is double)
Plamen Vurbanov A@b@c@example.com (only one @ is allowed outside quotation marks)
Plamen Vurbanov a"b(c)d,e:f;g<h>i[j\k]l@example.com (none of the special characters in this local part is allowed outside quotation marks)
Plamen Vurbanov just"not"right@example.com (quoted strings must be dot separated, or the only element making up the local-part)
Plamen Vurbanov this is"not\allowed@example.com (spaces, quotes, and backslashes may only exist when within quoted strings and preceded by a slash)
Plamen Vurbanov this\ still\"not\\allowed@example.com (even if escaped (preceded by a backslash), spaces, quotes, and backslashes must still be contained by quotes)
